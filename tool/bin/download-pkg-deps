#!/bin/sh
## Run this from <project-root>/tool/
set -e

# Install pkg in our home dir rather than globally so we can safely cache it in
# our CI environment.
[ -f ~/node_modules/.bin/pkg ] || (cd && npm install pkg@4.3.4)

clojure -R:uberjar -Stree
